{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the remote API client. Server access is exposed through typed Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
