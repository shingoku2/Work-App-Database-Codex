{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. Note: tauri-plugin-sql is registered in lib.rs but intentionally NOT granted to the frontend — all database access goes through the custom Rust commands in src-tauri/src/commands/ (see CLAUDE.md, dual-registration quirk).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
