# Keep Rust manifest and lock files LF-only on Windows.
# Cargo/Tauri tooling can rewrite these with LF endings;
# without this, Git for Windows with core.autocrlf=true reports fake dirty files.
*.toml text eol=lf
Cargo.lock text eol=lf
.gitattributes text eol=lf
